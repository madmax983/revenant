<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Schedule__c</fullName>
    <description>The Workflow Schedule this log entry belongs to. Logs are linked by this immutable Id (not by the mutable Name) so the audit trail stays attached when a schedule is renamed.</description>
    <externalId>false</externalId>
    <label>Schedule</label>
    <referenceTo>Workflow_Schedule__c</referenceTo>
    <relationshipLabel>Fire Logs</relationshipLabel>
    <relationshipName>Fire_Logs</relationshipName>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
